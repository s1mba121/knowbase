server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Match API multipart limit (20MB) with headroom
  client_max_body_size 25m;

  # SPA fallback
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Same-origin API proxy — frontend can use relative /v1 (empty VITE_API_URL)
  location /v1/ {
    proxy_pass http://api:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    # Preserve outer TLS (Cloudflare/host). $scheme here is always http (:80).
    proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
    proxy_set_header X-Forwarded-Host $http_x_forwarded_host;
    proxy_set_header Authorization $http_authorization;
    proxy_buffering off;
    proxy_read_timeout 120s;
    proxy_send_timeout 120s;
    client_max_body_size 25m;
  }

  location = /widget.js {
    proxy_pass http://api:3001/widget.js;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
    proxy_set_header X-Forwarded-Host $http_x_forwarded_host;
  }

  location = /health {
    proxy_pass http://api:3001/health;
  }

  location = /ready {
    proxy_pass http://api:3001/ready;
  }

  location = /metrics {
    proxy_pass http://api:3001/metrics;
  }

  gzip on;
  gzip_types text/css application/javascript application/json image/svg+xml;
}
