{
  "name": "knowbase",
  "private": true,
  "version": "0.1.0",
  "description": "Knowbase — docs to embeddable chatbot (fullstack MVP)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:web": "npm run dev -w knowbase-frontend",
    "dev:api": "npm run dev -w knowbase-backend",
    "build": "npm run build -w knowbase-frontend && npm run build:widget -w knowbase-frontend && npm run build -w knowbase-backend",
    "build:widget": "npm run build:widget -w knowbase-frontend",
    "typecheck": "npm run typecheck -w knowbase-backend --if-present && npm run build -w knowbase-frontend --if-present",
    "load:smoke": "node scripts/load-smoke.mjs",
    "test": "npm run test -w knowbase-backend",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "engines": {
    "node": ">=20"
  }
}
